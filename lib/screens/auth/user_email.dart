import 'package:flutter/material.dart';

class UserEmailScreen extends StatelessWidget {
  const UserEmailScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('User Email Screen'),
      ),
    );
  }
}
